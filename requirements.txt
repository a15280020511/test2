# Permanent control core: Python standard library only.
# Task-specific dependencies live under plugins/*/requirements.txt and are
# installed into disposable virtual environments only when that plug is used.
